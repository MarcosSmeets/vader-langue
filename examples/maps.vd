// maps.vd — maps (hash table) in the LLVM backend. Construction via newmap(); int or string key.

fn main() {
    map[string]int ages = newmap()
    ages["ada"] = 36
    ages["alan"] = 41
    ages["grace"] = 85
    print(ages["ada"])     // 36
    print(ages["alan"])    // 41
    print(len(ages))       // 3

    map[int]string names = newmap()
    names[1] = "one"
    names[2] = "two"
    print(names[2])        // two
}
